/**
 * Build-time constants replaced by Vite/esbuild's `define` option in
 * `vite.config.ts`. Values are inlined as string literals, so the
 * result is statically analyzable and tree-shakeable.
 */
declare const __BUILD_LOCALE__: 'en' | 'de';
